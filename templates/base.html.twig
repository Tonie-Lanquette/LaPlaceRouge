<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}

        <script src="https://cdn.tailwindcss.com"></script>
        <script src="https://kit.fontawesome.com/97cd5da9a0.js" crossorigin="anonymous"></script>

            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
        {# {% if is_granted('IS_AUTHENTICATED') %}  #}
            <header class ="flex w-100 h-24  font-bold ">
                {% if app.request.attributes.get('_route') in ['app_dishies_index','app_dishies_new','app_dishies_show','app_dishies_edit']  %} 
                    <a class="flex bg-white rounded w-full h-full items-center justify-around" href="{{ path('app_dishies_index') }}">Plats</a> 
                {% else %}
                    <a class="flex bg-black text-white hover:bg-red-200 hower:text-black rounded w-full h-full items-center justify-around" href="{{ path('app_dishies_index') }}">Plats</a>
                {% endif %}
                {# {% if app.request.attributes.get('_route') in ['app_table_index','app_table_new','app_table_show','app_table_edit']  %}
                    <a class="flex bg-white rounded w-full h-full items-center justify-around" href="{{ path('app_table_index') }}">Tables</a> 
                {% else %}
                    <a class="flex bg-black text-white hover:bg-red-200 hower:text-black rounded w-full h-full items-center justify-around" href="{{ path('app_table_index') }}">Tables</a> 
                {% endif %} #}
                {% if app.request.attributes.get('_route') in ['app_menu_index','app_menu_new','app_menu_show','app_menu_edit']  %} 
                    <a class="flex bg-white  rounded w-full h-full items-center justify-around" href="{{ path('app_menu_index') }}">Menus</a> 
                {% else %}
                    <a class="flex bg-black text-white hover:bg-red-200 hower:text-black rounded w-full h-full items-center justify-around" href="{{ path('app_menu_index') }}">Menus</a>
                {% endif %}
               {# {% if app.request.attributes.get('_route') in ['app_reservation_index','app_reservation_new','app_reservation_show','app_reservation_edit']  %}
                    <a class="flex bg-white  rounded w-full h-full items-center justify-around" href="{{ path('app_reservation_index') }}">Reservations</a> 
                {% else %}
                    <a class="flex bg-black text-white hover:bg-red-200 hower:text-black rounded w-full h-full items-center justify-around" href="{{ path('app_reservation_index') }}">Reservations</a> 
                {% endif %}
                    <a class="flex  bg-black text-white hover:bg-red-200 hower:text-black rounded w-full h-full items-center justify-around" href="{{ path('app_logout') }}">Déconnexion</a> #}
    {# {% else %} 
        
    {% endif %}  #}
    </header>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
