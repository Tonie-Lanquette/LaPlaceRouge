{{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="bg-[#DDBF93] py-2 my-6 px-3 ml-[85%] hover:bg-[#FFECEC] rounded-xl">{{ button_label|default('Envoyer') }}</button>

{{ form_end(form) }}
